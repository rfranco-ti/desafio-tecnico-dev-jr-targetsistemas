using System;

class FibonacciSequence {
    static void Main() {
        int number = 34, a = 0, b = 1;

        while (b < numero) {
            b = b + a;
            a = b - a;
        }

        if (b == number || number == 0) {
            Console.WriteLine("O número pertence à sequência de Fibonacci.");
        } else {
            Console.WriteLine("O número não pertence à sequência de Fibonacci.");
        }
    }
}